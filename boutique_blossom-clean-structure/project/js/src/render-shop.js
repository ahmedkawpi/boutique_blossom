/* ===== render-shop.js — storefront HTML: header, hero, categories, product grid, footer =====
   Pure render functions: take state, return an HTML string. No event
   listeners are attached here — that belongs in events-shop.js. */
