/* ===== state.js — the single global state object + pure formatters =====
   One source of truth (state = {...}). Formatters here (fmtPrice, fmtDate, t)
   should be pure functions: same input, same output, no side effects. */
