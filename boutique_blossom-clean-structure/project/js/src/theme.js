/* ===== theme.js — light/dark mode toggle + persistence =====
   Reads/writes theme preference only. Doesn't know about products, orders, or the DOM
   beyond the html[data-theme] attribute and its own toggle icon. */
