/* ===== events-boot.js — app startup: load data, initial render, top-level listeners =====
   The init() sequence and anything that only fires once on page load
   (theme init, scroll-reveal observer setup, initial data fetch). */
