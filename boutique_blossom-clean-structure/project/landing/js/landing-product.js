/* ===== landing-product.js — load + render the product: gallery, colors, sizes, thumbnails =====
   loadProduct(), renderProduct(), renderColorOptions(), renderThumbnails(),
   changeMainImage(). Everything about displaying the one product this page sells. */
