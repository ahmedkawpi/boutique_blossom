/* ===== landing-order.js — order form: delivery settings, validation, submit, success/error =====
   loadDeliverySettings(), updateAddressRequirement(), updateOrderSummary(),
   submitOrder(), showSuccess(), showError(). Everything about turning form
   input into a placed order. */
