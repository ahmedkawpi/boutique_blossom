/* ===== landing-tracking.js — Meta Pixel init/events + device id + order-limit guard =====
   Everything about *observing* the visitor: pixel events, per-device order
   count, showing/hiding the "order limit reached" state. No product or
   form logic here. */
